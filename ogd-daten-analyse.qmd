---
title: "ogd-daten-analyse.qmd"
format: html
editor: visual
author: Jana Kobler
---

## R Pakete laden

```{r}
library(readr)
library(dplyr)
library(ggplot2)
library(tidyverse)
devtools::install_github("statistikZH/statR")
```

```{r}
daten <- read_csv(here::here("daten/KTZH_00002423_00004863 (1).csv"))

glimpse(daten)
head(daten)
str(daten)
summary(daten)
names(daten)
colnames(daten)


```

```{r}
cols_to_keep <- sapply(daten, function(x) !any(x == "Nein"))
daten <- daten[, cols_to_keep]
daten<-select(daten, -ID)
daten_t <- as.data.frame(t(daten))
daten_t <- rownames_to_column(daten_t, var = "Variable")
column_names_row <- c("Variable", colnames(daten))
colnames(daten_t) <- daten_t[1,]
daten_t <- daten_t[-1, ]
rownames(daten_t) <- NULL
daten_t <- daten_t |> 
  rename(Werte = Indikator)
```

```{r}
ggplot(daten_t, aes(x=V1, y=Variable))+
  geom_point()

ggplot(daten_t, aes(x=V1, y=Variable))+
  geom_boxplot()

ggplot(daten_t, aes(x=Variable, y=V1))+
  geom_point()
```
